import styled from 'styled-components'

export const FormLink = styled.a`
    font-weight: bold;
    font-size: 12px;
    cursor: pointer;

    transition: 0.5s;

    &:hover {
        color: #ccc;
    }
`